language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"

install:
  - pip install -r requirements.txt --use-mirrors
  - pip install -r requirements-dev.txt --use-mirrors

script:
  - "flake8 *.py"
  - "pylint *.py"
